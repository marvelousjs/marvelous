/*
 * this file is automatically generated for marvelous via zappjs
 * any manual changes to this file will be overwritten with the "mvs" command
 */

import * as http from 'http';
import * as url from 'url';

interface I{{{serviceName}}}ServiceClientOpts {
  url: string;
}

interface I{{{serviceName}}}ServiceClient {
  {{#each calls}}
  {{{case @key 'camel'}}}: I{{{../serviceName}}}ServiceClient{{{case @key 'pascal'}}}Function;
  {{/each}}
}

{{#each calls}}
export interface I{{{../serviceName}}}ServiceClient{{{case @key 'pascal'}}}Function {
  (request?: I{{{../serviceName}}}ServiceClient{{{case @key 'pascal'}}}Request): Promise<I{{{../serviceName}}}ServiceClient{{{case @key 'pascal'}}}Response>;
}

{{#each this}}
{{{tsInterface (concat ../../serviceName 'ServiceClient' (case @../key 'pascal') (case @key 'pascal')) this}}}
{{/each}}
{{/each}}

async function handler(req: any = {}, urlString: string = ''): Promise<any> {
  return new Promise((resolve, reject) => {
    try {
      const reqString = JSON.stringify(req);
      const urlParts = url.parse(urlString);
      
      const input = http.request(
        {
          protocol: urlParts.protocol,
          host: urlParts.hostname,
          port: urlParts.port,
          path: urlParts.pathname,
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
            'Content-Length': Buffer.byteLength(reqString)
          }
        },
        output => {
          let responseString = '';

          output.on('data', chunk => {
            responseString += chunk;
          });

          output.on('end', () => {
            const response = JSON.parse(responseString) || {};
            if (output.statusCode === 200) {
              resolve(response);
            } else {
              reject(response);
            }
          });
        }
      );
      input.on('error', reject);
      input.write(reqString);
      input.end();
    } catch (error) {
      reject(error);
    }
  });
}

export class {{{serviceName}}}ServiceClient implements I{{{serviceName}}}ServiceClient {
  url = 'http://localhost:5000';

  constructor(opts?: I{{{serviceName}}}ServiceClientOpts) {
    if (opts && opts.url !== undefined) {
      this.url = opts.url;
    }
  }
  {{#each calls}}

  {{{case @key 'camel'}}}: I{{{../serviceName}}}ServiceClient{{{case @key 'pascal'}}}Function = async req => {
    return handler(req, `${this.url}/{{{case @key 'camel'}}}`);
  };
  {{/each}}
}
